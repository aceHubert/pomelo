{
  "name": "nestjs-identity",
  "description": "nestjs authorization",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "serve": "run -T concurrently --raw \"tsc --project tsconfig.json -watch\"",
    "build": "run -T rimraf dist && run -T tsc --project tsconfig.json"
  },
  "peerDependencies": {
    "@nestjs/common": "^9.0.0",
    "@nestjs/core": "^9.0.0",
    "@nestjs/graphql": "*",
    "graphql-parse-resolve-info": "^4.12.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "packageManager": "yarn@3.1.0"
}
